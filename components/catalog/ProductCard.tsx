import Link from 'next/link';import type {PublicOffer} from './PartnerOfferCard';export type PublicProduct={type:'TEST'|'PROFILE'|'PACKAGE';slug:string;name:string;description?:string|null;fastingNeeded:boolean;sampleTypes:string[];offers:PublicOffer[]};export default function ProductCard({product}:{product:PublicProduct}){const offer=product.offers[0],base=product.type==='TEST'?'tests':product.type==='PROFILE'?'profiles':'packages';return <article className="productCard"><span className="productType">{product.type}</span><h2>{product.name}</h2><p>{product.description??'View preparation, partner options and current availability.'}</p><div className="productMeta"><span>{product.fastingNeeded?'Fasting required':'No fasting indicated'}</span><span>{product.sampleTypes.join(', ')||'Sample details on request'}</span></div>{offer&&<p className="fromPrice">From <b>₹{offer.price}</b> • {offer.partner.name}</p>}<Link href={`/${base}/${product.slug}`}>View details and partners</Link></article>}
