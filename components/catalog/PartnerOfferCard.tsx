'use client';import ServiceabilityCheck from './ServiceabilityCheck';export type PublicOffer={offerId:string;partner:{slug:string;name:string;accreditation?:string|null};price:number;mrp?:number|null;discountPercent:number;availability:string;tat?:string|null};export default function PartnerOfferCard({offer,type,slug,onSelect}:{offer:PublicOffer;type:string;slug:string;onSelect?:(pincode:string)=>void}){return <section className="offerCard"><div><strong>{offer.partner.name}</strong>{offer.partner.accreditation&&<span className="verified">Verified: {offer.partner.accreditation}</span>}</div><div className="offerPrice">{offer.mrp&&<del>₹{offer.mrp}</del>}<b>₹{offer.price}</b>{offer.discountPercent>0&&<span>{offer.discountPercent}% off</span>}</div><p>{offer.tat??'TAT confirmed before booking'}</p><ServiceabilityCheck type={type} slug={slug} offer={offer} onSupported={onSelect}/></section>}
