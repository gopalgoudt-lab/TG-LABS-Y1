import {notFound} from 'next/navigation';import {prisma} from '@/lib/prisma';export const dynamic='force-dynamic';export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params,value=await prisma.catalogCategory.findFirst({where:{slug,active:true},select:{name:true,description:true}});if(!value)notFound();return <main className="catalogDetail"><a href={`/?category=${slug}`}>← View filtered catalog</a><span className="productType">CATEGORY</span><h1>{value.name}</h1><p>{value.description}</p><a className="catalogCta" href={`/?category=${slug}`}>Browse eligible products</a></main>}
