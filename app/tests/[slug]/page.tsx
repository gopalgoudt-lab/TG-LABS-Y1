import {notFound} from 'next/navigation';import CatalogDetail from '@/components/catalog/CatalogDetail';import {findPublicTest} from '@/lib/catalog-data';import {testDetailsDto} from '@/lib/catalog-public-dto';export const dynamic='force-dynamic';export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params,value=await findPublicTest(slug);if(!value)notFound();return <CatalogDetail product={testDetailsDto(value)}/>}
