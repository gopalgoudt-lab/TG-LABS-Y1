import './globals.css';
import type { ReactNode } from 'react';
export const metadata={title:'TG Labs — Better Diagnostics. Better Decisions.',description:'Diagnostic tests, health packages, home sample collection and secure digital reports.'};
export default function RootLayout({children}:{children:ReactNode}){return <html lang="en"><body>{children}</body></html>}
