import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';import {categoryDto} from '@/lib/catalog-public-dto';export const dynamic='force-dynamic';export async function GET(){try{const rows=await prisma.catalogCategory.findMany({where:{active:true},orderBy:[{sortOrder:'asc'},{name:'asc'}],select:{slug:true,name:true,description:true}});return NextResponse.json({categories:rows.map(categoryDto)});}catch{return NextResponse.json({error:'Categories are temporarily unavailable.'},{status:503});}}
