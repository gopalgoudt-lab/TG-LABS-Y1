import {NextResponse} from 'next/server';import {findPublicTest} from '@/lib/catalog-data';import {testDetailsDto} from '@/lib/catalog-public-dto';export const dynamic='force-dynamic';export async function GET(_r:Request,{params}:{params:Promise<{slug:string}>}){try{const {slug}=await params;const value=await findPublicTest(slug);return value?NextResponse.json({product:testDetailsDto(value)}):NextResponse.json({error:'Not found.'},{status:404});}catch{return NextResponse.json({error:'Catalog is temporarily unavailable.'},{status:503});}}
