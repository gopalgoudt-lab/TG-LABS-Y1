import {notFound} from 'next/navigation';import CatalogDetail from '@/components/catalog/CatalogDetail';import {findPublicPackage} from '@/lib/catalog-data';import {packageDetailsDto} from '@/lib/catalog-public-dto';export const dynamic='force-dynamic';export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params,value=await findPublicPackage(slug,'PACKAGE');if(!value)notFound();return <CatalogDetail product={packageDetailsDto(value)}/>}
