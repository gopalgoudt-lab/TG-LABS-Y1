import assert from 'node:assert/strict';import test from 'node:test';import fs from 'node:fs';test('catalog cards prioritize selling price and discount on mobile',()=>{const d=fs.readFileSync('components/catalog/ProductCard.tsx','utf8');const css=fs.readFileSync('app/homepage-mobile-review.css','utf8');assert.match(d,/catalogSellingPrice/);assert.match(d,/% OFF/);assert.match(d,/<s>₹\{offer\.mrp\}<\/s> MRP/);assert.match(d,/catalogPriceMeta/);assert.match(css,/\.catalogSellingPrice/);assert.match(css,/\.catalogDiscount/);});