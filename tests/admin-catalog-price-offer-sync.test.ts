import assert from 'node:assert/strict';import test from 'node:test';import fs from 'node:fs';test('admin catalog price and MRP updates sync to selected partner offer',()=>{const d=fs.readFileSync('app/api/admin/catalog-editor/[kind]/[id]/route.ts','utf8');assert.match(d,/body\.price !== undefined \|\| body\.mrp !== undefined/);assert.match(d,/tx\.testPartnerOffer\.updateMany/);assert.match(d,/tx\.packagePartnerOffer\.updateMany/);assert.match(d,/price: body\.price/);assert.match(d,/mrp: body\.mrp/);});