import assert from 'node:assert/strict';import test from 'node:test';import fs from 'node:fs';test('home catalog cards show MRP, selling price and discount when applicable',()=>{const d=fs.readFileSync('components/catalog/ProductCard.tsx','utf8');assert.match(d,/offer\.mrp > offer\.price/);assert.match(d,/MRP <s>₹\{offer\.mrp\}<\/s>/);assert.match(d,/From <b>₹\{offer\.price\}<\/b>/);assert.match(d,/discountPercent/);assert.match(d,/% off/);});