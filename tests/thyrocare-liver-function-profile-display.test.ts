import assert from 'node:assert/strict';import test from 'node:test';import fs from 'node:fs';test('Liver Function Tests displays all 12 parameters',()=>{const d=fs.readFileSync('components/catalog/CatalogDetail.tsx','utf8');assert.match(d,/product\.id==='thyrocare-profile-lft-54'/);for(const name of ['Bilirubin - Indirect','Serum Albumin / Globulin Ratio','SGOT / SGPT Ratio'])assert.match(d,new RegExp(name.replaceAll('/','\\/')));assert.match(d,/12-parameter profile total/);});